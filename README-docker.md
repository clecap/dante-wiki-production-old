# Remarks for Installing Docker

## On Debian

* ```adduser  --disabled-password  dante```
* Check shell: We prefer bash over tsh
* curl is not in path

* https://docs.docker.com/engine/install/linux-postinstall/#manage-docker-as-a-non-root-user

